import { useEffect, useState } from 'react'
import { Link, useLocation, useNavigate } from 'react-router-dom'
import { getDevAuthState, setDevAuthState, type DevAuthState } from './auth'
import { devScenarios, getDevScenario, setDevScenario, type DevScenario } from './scenarios'
import './dev.css'

const routes = [{ path: '/', label: '홈' }, { path: '/reservations', label: '예약 조회' }, { path: '/my-reservations', label: '내 예약' }, { path: '/__dev/components', label: '컴포넌트' }]
export function DevShell() { const navigate=useNavigate(); const location=useLocation(); const [open,setOpen]=useState(true); const [scenario,setScenario]=useState<DevScenario>(getDevScenario()); const [auth,setAuth]=useState<DevAuthState>(getDevAuthState()); useEffect(()=>{const sync=()=>{setScenario(getDevScenario());setAuth(getDevAuthState())};addEventListener('msds-dev-scenario',sync);addEventListener('msds-dev-auth',sync);return()=>{removeEventListener('msds-dev-scenario',sync);removeEventListener('msds-dev-auth',sync)}},[]); if(!open)return <button className="dev-fab" onClick={()=>setOpen(true)}>DEV</button>; return <aside className="dev-shell" aria-label="개발 도구"><header><strong>DEVTOOLS</strong><button onClick={()=>setOpen(false)} aria-label="개발 도구 접기">×</button></header><section><h2>ROUTES</h2>{routes.map(route=><button className={location.pathname===route.path?'selected':''} key={route.path} onClick={()=>navigate(route.path)}><span>{route.label}</span><code>{route.path}</code></button>)}</section><section><h2>AUTH</h2><select aria-label="인증 상태" value={auth} onChange={e=>{const next=e.target.value as DevAuthState;setDevAuthState(next);setAuth(next)}}><option value="guest">비로그인</option><option value="member">로그인됨</option></select></section><section><h2>API SCENARIO</h2><select aria-label="API 시나리오" value={scenario} onChange={e=>{const next=e.target.value as DevScenario;setDevScenario(next);setScenario(next)}}>{devScenarios.map(item=><option key={item.id} value={item.id}>{item.label}</option>)}</select><p>변경 후 화면의 조회·확정·취소 동작을 다시 실행하세요.</p></section><Link to="/__dev/components">OPEN COMPONENTS →</Link></aside> }
